server:
  port: 8040

eureka:
  instance:
    hostname: bankatii-transactionservice-server.onrender.com
  client:
    serviceUrl:
      defaultZone:
        ${EUREKA_HOST:https://bankatii-discovery.onrender.com/eureka/}
    register-with-eureka: true
    fetch-registry: true
  server:
    enable-self-preservation: false